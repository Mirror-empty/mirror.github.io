# 12月

### 12.26

考考考 考个der 打开题目一看 啥都不会 还好有兄弟们 不然凉透。
希望学习通的只是进行网页截屏。

做了个根据数组来判断是不是后序遍历，左、右、根，而根据二叉搜索树的特点，数组最后一个是root 左<根<右，因此只要找到每个节点的根就行，然后用递归来解决。

lottey第一此学习，跑那个dubbo之间的远程调用，有个bug，已记录。rpc也是一种分布式架构，dubbo就是远程调用，emm没有了解很多，今天也只是把别人的代码先跑起来。

MVCC 多版本并发控制，一种隔离级别的具体实现，就是用事务id去判定此事务的级别，别的事务是否可以访问。以避免发生读脏数据和不可重复读。


### 12.27

今天干了啥呢？

上午写一个判断二叉树到叶子节点的和是否target相等，向这种要寻找所有节点的用回溯暴力破解，for对应行，递归来对应深度，用path来做为回退，然后还写了个思路一样的回溯题。

lottery使用本地搭建起来，了解整个项目的基础架构，应用，领域，基础，领域，对外暴露的rpc，对于这些只是初次了解，明天继续。

http中连接的方式与Cookie的用途，怎么创建与安全，对比Session，浏览器中的缓存，一说这个，贼宝，Edge如果不去清缓存就会导致c盘爆炸，

Next-key Locks+MVCC去解决幻影读的问题，幻影读就是一个事务去读另一个事务时，它发生了区间的更改，导致两次读的不一样，和重复读道理一样。

看了ArrayList源码，默认空间为10，然后如果add会创建一个最多原1.5倍空间大小，移除一个元素消耗时间为O(n),transient关键字的修饰，声明改变量不能被序列化，不能修饰方法和类。源码还没看完。
设计模式，解释器，啥也没解，只是了解个概念。


### 12.28

今日学习：
上午照样一个剑指offer题，将搜索二叉树转换成双向的循环链表，用中序遍历即可，两个全局变量，一个指向头部，一个指向随递归二走，只有把图画出来这题就好做了。然后吃了个涨停，根本就没有心思做，还好下午调整过来了。下午就下了一下午的雪。

数据库函数依赖复习，第一第二三范式，根本就是去除数据的冗余，基本上是通过表与表的联系来去除，ER图记得三个实体、属性、联系，一对多，多对多，一对一就行，实体之间。

lottery这块，运用了策略模式来编写代码的架构，先去编写一个算法总提的接口，然后两个实现类继承这个接口，根据策略ID去分别，不过在项目中抽奖的奖品概率是固定的，随机获取索引值，如果通过循环来遍历时间复杂度为O(n),这种是不推荐的，运用在总体概率中，另外一种是斐波拉契散列增量，通过哈希去计算下标，来进行初值的存储，不过这种计算公式的是固定的。通过随机SecureRandom类来随机，然后去元组进行索引。主要还是算法在项目中和设计模式到项目中的运用。

HTTP这块将昨天剩下的弄完了，也明白网关是将HTTP转换为其他协议进行通信的作用，因此rpc也是一样的，反向代理是代理到服务器上的，所以我们是查不到的，正向代理是代理到客户端上的，本机。

双指针无碍了，只要能知道题目的边界就行。今天javalinkedlist源码还没看，明天在弄，然后项目中的不常见的类也没有去做笔记。一个SecureRandom，一个ConcurrentHashMap都是关于安全的。

明天弄抽奖第六章，数据库复习完了，加个Socket的学习。

写个总结，都要细敲慢理，看来是没有学到心里去，不能流畅的敲出来，想到什么就写什么呀，磨磨唧唧的。


### 12.29

又到写总结的时候，速度干完去休息。

上午HTTPS去怎么去加密的，CA认证，没了，然后就是项目，模板方法的应用，整个抽奖的初步流程，这种低耦合的代码看着真的舒心，今天也只是把昨天的代码重构一样，使用阿里巴巴的开发规范，一些重要的流程与实现都是在abstract修饰的类中，抽象类没有必要去继承接口的所有方法，因为接口也是抽象类的一种体现，只有类继承才要，模板方法把流程的参数都是变量，方便扩充，也封闭了逻辑的具体实现，只要执行方法就行。

算法二叉树总之就是递归的实现然后四个遍历的理解，回溯照样是for+back，主要是怎么去remove。

linkedlsit和arraylist的区别，用几w的数据进行测试，插入头部，中间，尾部的问题。还有五中遍历，for增加，迭代器，strem新特性。hashmap了解整个存储的结构，hashcode的计算与上面要选择31，奇质数，碰撞小。

网络的基础概念，就是书本的第一章一样，有什么架构，怎么计算传输，分组通信。

明天项目干第七章，其他照样。




### 12.30

刷算法题不能为了做题而做题，应该给自己设定一个时间的。今天还是二叉树与回溯。

HTTP复习完了，还记得什么呢，几个请求方法，响应码？HTTPS加密？get与post的区别，请求的流程？还是要去看。 首部 具体应用Cookie与Session。

hashmap源码，为什么要定义16位初始值，它的扰动函数，阈值，怎么扩容的。

工厂模式的运用，定义工厂类向外扩充，什么变量用枚举是最好的。

迭代器设计，聚合里面去调用迭代，迭代里面实现顺序。

好像今天没做什么哟，从九点半到十一点，从一点到四点，从六点到七点，现在是八点半
总共十一个小时，可能学了有五个小时吧，还有五个小时干嘛去了？看微信，聊天？



### 12.31

算法还是很重要的，明天周赛试一试，今天弄了回溯，总的来说，去抽象画图，还是有作用的，尽可能的相同更多的方法，剪枝还是不晓得，可以理解成去除多余的步骤?主要还是选与不选的问题，学一学深度和广度

项目活动领域去学习状态模式，其实学的时候应该想一想你会怎么做，如果是你的接这个业务你会怎么写，状态模式，根据去入参的值去选择状态。封装吧，要求学学几个原则，这样子看是不行的。报bean问题，就是有文件没导到bean中，贼长的一系列红，没入@service注解。

链路层，数据的传输，同C/AD什么协议，有载波，监听的功能，还有就是复用，什么以太坊，什么局域网，一样基本词语解释。

hashmap源码还是没看，看个vector有个的用啊，和arraylist的区别就是加了synchronize，同步添加，学一学Arrays和Collections。

2022年结束了，日历翻页了。

### 1.1

今天任务没弄完，不想做咯怎么办。

上午打个周赛，难怪这么简单，连我都能做成两个，而然一到晚上做题目就露出原型。

晚上眼睛疼，根本就没有去思考。还是今天早上去玩手机，没有睡觉的缘故。

分布式id算法，这都是简历上的项目经历啊，雪花算法也没有去看，一个Arrays，一个Collections，方法也没看，还弄了个设计原则，接口原则，单一原则，依赖导致，一个接口分配一个单一的业务，接口实现去改变。

总的来说，就是打了个周赛飘了，今天啥也没学。

[![pSPRpkQ.png](https://s1.ax1x.com/2023/01/02/pSPRpkQ.png)](https://imgse.com/i/pSPRpkQ)


### 1.2

今天还是有点东西的，路由分库分表，自己去写一个还是很有难度的，涉及的知识点也好多，以前学过的，也忘记了，以前不知道AOP的作用，日志，增加方法，其实就是开发组件时用的，还有MyBatis拦截器，一些注解和类的使用。怎样的一个流程也要去理清楚，明天画图，还有添加字段进行分库分表，小作业，有时间可以做呀。

回溯，begin，与used去控制的区别，将他们全部打出来，还是很好理清的，现在就要去做动态规划了，还要复习十大排序。

网络这块没怎么搞，子网划分？ARP协议？

今天主要还是这个路由组件，RPC分布式，这里以前根本就没学哟。明天弄个demo

en明天还要去做简历，面向简历复习才是好的，这样对着敲，其实没记住什么东西。


### 1.3

这个八股文还没开始被哟，是先敲这个基础，还是被八股文呢，八股文还是要理解好点。还要弄项目，rpc的学习，dutton的学习，算法的学习，实际上还挺多的，2月份开始就要投简历了，半个月把项目弄完，动态规划，并查集，然后开始springboot中间件的学习，先把八股文背了，算法，项目，rpc的理解不落下就行。

跟着群主走就行，等到压力位就遛，不能多留。

rpc只是一种远程调用的手段，而现在学的就是如何去造这个东西，AOP的理解，切面，MyBatis拦截器，难怪要看源码，dp主要还是能把规律找出来，边界值的判定，threadlocal类上下文，编程性事务声明吧

网络，网络转ip协议ARp，路由传递协议，RIP，OSPF，网际报文控制ICMP


### 1.4

今日学习：dp问题还是有去寻找规律，今天这题你就没有想出来，不过也算是了解了质因数。

项目的今天一章差不多看了两三个小时吧，弄清楚了什么是事务，编程式事务，然后声明式的注解还没有做笔记，然后一般的业务一个逻辑怎么去写，抽象类中实现流程，实现类实现dao层的逻辑和判断，支持类提供数据。事务的这么运行。spring事务

hashmap明天源码还是要看的，不能在拖了，TCP，UDP，三次握手，四次挥手，滑动窗口，拥塞，怎么解决

回溯，组合，排列，子集，子集用for来控制，重复就用used布尔，当前与前一个进行判定，i+1就不去看前一个了。

又是去玩了。

世界上最无耻最阴险，最歹毒的赞美，就是用穷人的艰辛和苦难，当做励志故事来愚弄底层人。


### 1.5

今日老姐他们突然回来了，中午还要去吃席，搞得上午一直到中午三点才去学习。今天也没有做什么，一个流程编排，将前面学的所有逻辑都串连起来，主要还是字段的设计与VO，RES，REQ的设计，这才是难点。

然后算法，慢慢来把，每天只做那么多，虽然还是do做不出，但还是要花时间去学习，一天2个小时去写吧，毕竟，你写了这么多，dp，回溯，二叉树，链表，都还是有点印象的。不至于下不来了笔。

javap 反编译，网络层，String.initer方法，线程池。运算的隐式转换。

明天还是要继续学习，不要管那么多，对面回你是自由，回不回对你有影响吗？做好自己的事才是正道，那么多的东西要学。


### 1.6

今日组合模式中规则量化的学习，主要还是表的搭建和vo，res，req，聚合对象的创建还是有难点，决策二叉树，一种组合模式的运用。

应用层的一些协议，域名系统，FTP，SMTP。

明天进行操作系统的复习，或者Socket，的学习，还是按照简历上的项目来学习吧，他瞄的 手机摔了下 卡移位了 还好没事

明天学习分布式，反正简历上的东西2月份起都是要先过一遍的。

算法，dp还是有点难度的，主要是规律找出来，边界不好搞，先学贪心吧，只需要满足一小部就行，不要全部都满足。局部最优

关键字，final，this，Object类方法，底层实现，nactive 第一个关键字，表示c实现。

hsahmap的底层原理，数组加链表，链表默认为8并且数组小于64就有可能转去红黑树。


### 1.7

贪心还是很好做的，找到局部最优，意思是怎么去找到能最好的去满足要求，比如那个不重复区间，找末尾最小的，尽量给后面留位置，为后面的人找想。

kafka，一些基本的吧，分布式系统与微服务架构，互存关系。很多中间件都是分布式的中间件，而这些中间件是可以写出来的，怎么写出来就用到了算法和设计模式的重点。算法刷题，应该学会的是它的思想。贪心思想，找到局部最优解，和起来就变成全局的最优了。

在接口层去调用应用层的方法，接口层对外暴露。

然后今天没有什么扩展。只是把该做的任务做完了。

### 1.8

周赛也就看了下，写了第一个中等题，多测试测试，看能不能稳定在两道题目。

项目今天报错搞了三四个小时，还是流程的不熟悉，哪里变更了不知道，不过，自己的排错能力还是提高了，只要能找到具体的错误，就好解决了。

分布式中CAP理论，一致性，并发性，网络容错性，都是很重要的，CP与AP中的取舍，今天也听了一下模拟面试，这些理论还是要有的，比如消息中间件的区别，区别是怎么实现的才是重要的。

算法，贪心，和昨天的题目类似，局部最优来导致全局最优，还有并查集，没怎么看，主要是能写个测试类学一下，就通俗易懂了。

java基础过了一遍，下面去准备并发，然后JVM了。

### 1.9

又是一天，还有二十天投简历，中间起码有十天学习时间是很短的。明天要准备操作系统的知识了还有并发

今天并查集是弄明白了，指向父节点，一棵树，贪心还是要去明白题目的意思，找到题目的最优解。

项目快要完成了，先写在简历上，然后在对着简历去复习与修改，今天学的是xxl-job，只能说有一点了解吧，定时的去执行，比它低的一个定时quartz。

只要写在简历上的东西你就要去做好扩展的准备。


### 1.10

又是一天咯，项目明天就要完结了，今天就是轻轻理解MQ的幂等性，然后MQ的消峰填谷，解耦，消息重复怎么办，要想一想项目完结后应该这么走了。

操作系统 整个概述，算是起步了，Java线程的创建和一些线程的基础机制。java面经今天没干，分布式Raft共识性算法。

算法KMP，next数组的内容这么创建，前缀和后缀，其实也是半知半解的样子，没有完全领会。贪心还是不够，第二题写不出。

### 1.11

没必要去抱怨今天又过完了，只要今天学到了知识就行，没必要这么焦虑

项目时基本上完成了到底学会了什么呢，我个人觉得可以做成一个知识图，融会贯通，你现在的话对于项目只是了解一点点的，redis分布式锁的应用setnx，简单的锁住，还有myql 悲观锁，每次事务的更改都任务被修改了，因此都是加锁，乐观锁，加个版本控制字段，每次认为没有什么改变，然后消息发送成功要进行消息回调，这个弄了半天。因为消息重复使用的问题。

操作系统的信号量PV操作不熟悉，进程与线程的区别，没有理解到脑袋中去。线程的中断。

算法贪心还是不够熟练，主要是你没有去找到它的思想，局部最优解，比如股票二这个题目？总体的利润要等于每天与明天的利润之和？到时候多练常规题吧。

### 1.12

快排还是很深奥的，优化对于升降序列选择中间是最好的，递归做法的边界值，直接用模板，非递归还没有，不过一样是分治，将数组分成两份。贪心每次去思考从小到大吧。明天开始深度和广度。

今天项目没怎么动，erp系统，企业内部，明天弄

操作系统复习完了，只是了解了进程和线程，进程调度算法，进程通信，管道，一些常见的面试题还是要去记住的。

并发，线程的状态，JUC，JDK，JVM区别，synchronize的区别和应用。

分布锁，Ression去封装了怎么去锁，lua脚本的运行，数据库唯一索引，建立唯一约束，

分布式事务，TCC，seata中间件去实现，TA，XT，数据库中的索引，刚性事务和柔性事务，过一会一致性，AP，2PC，本地消息表，saga，一个事务去触发另一个事务。

### 1.13

变天了，加了个群，实时java是真的卷。

项目erp学了吧，下一步就把nacos给装上去，DDD的分层吧，找个时间把项目给写了。

并发复习完了，futuretask，CAS，JUC并发编程类，线程的一些状态死锁啊，轻量锁，操作系统的I/O中进行Stocket通信，将内存中的数据放到应用进程中去。

算法DFS+BFS模板去弄懂了，每天写力扣上的热题对应的考的多的热题，环形链表，第几个k节点，环节点在哪个位置，都可以用快慢指针和hashset。

缓存 缓存穿透，缓存雪崩。